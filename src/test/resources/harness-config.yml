inputFormat: xml
context: testContext

databasesUnderTest:
  - name: snowflake
    version:
    url: jdbc:snowflake://uba05197.snowflakecomputing.com/?db=lbcat&schema=public
    username: OKUSHN
    password: LiquibasePass1
    dbSchema: PUBLIC
   # paste you snowflake account id and credentials. URL format described here
  # https://docs.snowflake.com/en/user-guide/jdbc-configure.html#connection-parameters
  # SnowflakeChangeObjectIT requires DB pre-populated with data use sdk.test.initScript.sql